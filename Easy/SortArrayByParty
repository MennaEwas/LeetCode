class Solution:
    def sortArrayByParity(self, A: List[int]) -> List[int]:
        le = []
        lo = []
        for i in range(len(A)):
            if A[i]%2 == 0:
                le.append(A[i])
            else:
                lo.append(A[i])
        return le + lo
